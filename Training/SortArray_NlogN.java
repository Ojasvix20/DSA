// task- sort array in O(nlogn) and least space complexity - RECURSIVE INSERTION SORT

public class SortArray_NlogN{
    
    public static int[] sortArr(int[] arr){
        List<String> arrL = new ArrayList<>(Arrays.asList(arr));
        int num1 = arrL.get(arrL.size()-1);
        arrL.remove(arrL.size()-1);
        // INCOMPLETE
    }

}